{
  "name": "store",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
  "start": "node server.js",
  "heroku-postbuild": "cd src && npm install && npm run build && mv ../dist/store/browser ../server/public"
},
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "stripe": "^10.17.0"
  }
}
